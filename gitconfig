[core]
	editor = vim
	autocrlf = input
[user]
	name = Matthew Harrigan
	email = matt.harrigan@rigetti.com
[push]
	default = simple
[alias]
    wdiff = diff --color-words --ignore-all-space
    ldiff = difftool -y -t latex
[difftool.latex]
    cmd = latexdiff "$LOCAL" "$REMOTE"
[credential]
	helper = cache --timeout=3600
[diff]
    tool = meld
